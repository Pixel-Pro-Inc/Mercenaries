using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectModels : MonoBehaviour
{
    //Here is where the magic happens, I have left this empty because i want to build the tools and entities to play with over here. This is the Playground
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
